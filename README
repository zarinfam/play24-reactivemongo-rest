play24-reactivemongo-rest
=================================

This is a sample project that use Play Framework 2.4, Scala and ReactiveMongo to provide REST service.
This sample uses JSON <-> BSON conversion feature and JSON Writes and Reads feature in two separate controller.